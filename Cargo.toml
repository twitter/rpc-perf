[workspace]
members = [
    "atomics",
    "buffer",
    "codec",
    "datastructures",
    "logger",
    "metrics",
    "metrics-core",
    "ratelimiter",
    "rpc-perf",
    "timer",
    "waterfall",
]

[profile.bench]
debug = true
lto = true
codegen-units = 1

[profile.release]
debug = true
lto = true
codegen-units = 1
